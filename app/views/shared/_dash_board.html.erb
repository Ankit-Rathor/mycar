<nav class="navbar navbar-expand-lg navbar-light bg-dark">

  <div class="container-fluid">
    <% if user_signed_in? %>
    <%= button_to "Log Out", session_path(current_user.id), method: :delete %>
  <% end %>
    <div class="collapse navbar-collapse" id="navbarText">
      <ul class="navbar-nav me-auto mb-2 mb-lg-0">
      <% if current_user.customer? %>
        <li class="nav-item m-4">
          <%= link_to "Booking", new_car_path %>
        </li>
         <li class="nav-item m-4">
          <%= link_to "profile",  user_path(current_user.id)%>
        </li>
          <li class="nav-item m-4">
          <%= link_to "cars",  cars_path(current_user.id)%>
        </li>
          <li class="nav-item m-4">
          <%= link_to "Booking_Details", bookings_path(current_user.id)%>
        </li>
        <% elsif  current_user.admin?%>
           <li class="nav-item m-4">
          <%= link_to "Service_History", service_history_path%>
        </li>
        <li class="nav-item m-4">
          <%= link_to "Booking", admins_path%>
        </li>
           <li class="nav-item m-4">
          <%= link_to "Profile", admin_profile_path %>
        </li> 
          <% elsif  current_user.mechanic?%>
           <li class="nav-item m-4">
            <li class="nav-item m-4">
          <%= link_to "profile",  user_path(current_user.id)%>
        </li>
        </li>
        <li class="nav-item m-4">
          <%= link_to "Booking", mechanic_booking_path%>
        </li>
        <% end %>
      </ul>
      
    </div>
  </div>
</nav>
  <% flash.each do |type, msg| %>
      <div class="alert alert-info">
        <%= msg %>
      </div>
    <% end %>