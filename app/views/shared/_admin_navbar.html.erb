<nav class="navbar navbar-expand-lg navbar-light  sticky-top bg-dark">
  <div class="container-fluid">
    <% if user_signed_in? %>
    <%= button_to "Log Out", session_path(current_user.id), method: :delete %> 
  <% end %>
    <div class="collapse navbar-collapse" id="navbarText">
      <ul class="navbar-nav me-auto mb-2 mb-lg-0">
         <li class="nav-item m-4">
          <%= link_to "Home", new_admin_path%>
        </li>
        <li class="nav-item m-4">
          <%= link_to "Service_History", service_history_path %>
        </li>
        <li class="nav-item m-4">
          <%= link_to "Booking", admins_path%>
        </li>
           <li class="nav-item m-4">
          <%= link_to "Profile", admin_profile_path %>
        </li>
        <li class="nav-item m-4">
          <%= link_to 'show_mechanics', { controller: 'mechanics', action: 'index'} %>
        </li>
      </ul>
    </div>
  </div>
</nav>
<% flash.each do |type, msg| %>
  <div class="alert alert-info">
    <%= msg %>
  </div>
<% end %>


